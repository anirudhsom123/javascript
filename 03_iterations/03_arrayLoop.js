// for of loop

const arr=["ironman","captian america","thor"]

for (const hero of arr) {  
   // console.log(hero);
}

const map=new Map();
map.set('a',"anirudh")
map.set('b',"ball")
map.set('c',"cat")

for(const [key,value] of map){
   // console.log(`${key} :- ${value}`)
}


